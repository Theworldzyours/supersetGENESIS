{
  // NOTE: This Codespace is for CODING ONLY - no local service execution.
  // All running/testing happens on external infrastructure (Render, etc.)
  "extends": "./devcontainer-base.json",

  "name": "Apache Superset Development (Codespaces - Code Only)",

  // No port forwarding - we don't run services locally
  "forwardPorts": [],

  // Run commands after container is created (dev tools only)
  "postCreateCommand": "bash .devcontainer/setup-dev.sh || echo '⚠️ Setup had issues - run .devcontainer/setup-dev.sh manually'",

  // NO auto-start - Codespace resources are for coding, not running services

  // Set environment variables
  "remoteEnv": {
    // Codespace is for development only
  },

  // NOTE: VS Code customizations/features come from devcontainer-base.json
}
